import type { VariantNameOverride } from "./types";

/**
 * Add manual variant name overrides here when the source data
 * uses a different name than the one expected by the app.
 */
export const variantNameOverrides: VariantNameOverride[] = [
	{ from: "decoration", to: "Decoration" },
];
