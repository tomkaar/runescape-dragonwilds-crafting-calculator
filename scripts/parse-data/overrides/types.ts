export type RecipeImageOverride = {
  output: string;
  usesMaterial?: string[];
  image: string;
};

export type ItemImageOverride = {
  itemName: string;
  image: string;
};

export type FacilityNameOverride = {
  from: string;
  to: string;
};
